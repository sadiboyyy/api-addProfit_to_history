const numbers = []
const history =  []

function addNumber(number) {
  numbers.push(number)
  
}

function sumNumbers() {
  let sum = 0
  let i = 0
  
  while (
    
    i < numbers.length
    
  ) {
    console.log(i)
    sum = sum + numbers[i]
    console.log(numbers[i])
    i++
    return sum 
   }
 sum
  
}
addNumber(25)
addNumber(-10)
addNumber(-1)
addNumber(-900)
numbers
console.log(numbers[0])

sumNumbers()


addIncomeHistory(-4)
